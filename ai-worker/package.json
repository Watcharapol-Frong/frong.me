{
  "name": "ai-assistant-worker",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "test": "node test/auth.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
